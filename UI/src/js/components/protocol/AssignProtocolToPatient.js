import React, {Component} from 'react';

class AssignProtocolToPatient extends Component {
    render() {
        return (
            <div>
                AssignProtocolToPatient (BUILDING)
                depois enviar para a principal (facilzinho cara)
            </div>
        )
    }
}

export default AssignProtocolToPatient;
